# Canvas

The canvas will let users add images and text onto an infinite canvas.

Users should be able to copy and paste images onto a canvas.

The canvas should let users turn on a "tiling mode" which will automatically tile the pasted images together.